Keep the single-file frontend in index.html and the existing backend. Do not create new directories or config files. Implement these 3 specific updates:

1. Rebranding to 'Sahayog' & Asset Logo:

Change the application name throughout the UI (navbar, hero, page <title>, footer, toasts) from VelocityGig to Sahayog.

In the navbar, replace any text placeholder or default icon with our actual logo image located at assets/logo.png (or .svg / relative path). Set an appropriate height (around 36px to 42px) and add alt="Sahayog Logo". Place the brand name Sahayog neatly next to it using 'Baloo Bhai 2'.

2. Dual-State Auth Modal (Login & Sign Up):

Upgrade the existing auth modal to contain two toggle tabs at the top: 'Log In' and 'Sign Up'.

Add a smooth slide/fade transition when switching between the two tabs.

Login Tab: Fields for Email/Phone and Password, with a 'Log In' button. Include a bottom helper text: "Don't have an account? Sign up" that flips to the Sign Up tab.

Sign Up Tab: Fields for Full Name, Phone Number, Role (Client / Sahayogi Worker), and Create Password, with a 'Create Account' button. Include bottom helper text: "Already have an account? Log in".

3. Working Frontend Mock Auth Logic:

Keep it purely client-side using localStorage (do not waste time wiring complex database authentication).

Submitting either form should:

Save isLoggedIn: true, the user's name, and their selected role into localStorage.

Trigger a success toast ("Welcome to Sahayog, [Name]!").

Close the modal smoothly.

Update the navbar with the user's name, avatar initials badge, and a functional Logout button.

Ensure clicking Logout clears localStorage, shows a toast ("Logged out successfully"), and reverts the navbar back to the 'Log In / Sign Up' button.

Maintain all prior fixes (no broken DOM element IDs or unhandled event listener errors)."