Keep the existing 3-file structure (index.html, app.py, requirements.txt). Do not create new files. Apply the following 4 updates directly to index.html:

1. Fix the Hero Buttons:

Ensure the 'Book a Service' and 'Become a Worker' buttons in the Hero section work properly on click.

'Book a Service' should smoothly scroll to the booking section (#booking-section or equivalent) and activate the Booking tab.

'Become a Worker' should smoothly scroll to the worker registration section and activate the Worker tab.

Fix any broken event listeners or mismatched target IDs in the JavaScript.

2. Font Change (Baloo Bhai 2):

Import Google Font 'Baloo Bhai 2' via a <link> tag in the <head> of index.html.

Set font-family: 'Baloo Bhai 2', sans-serif; globally across the entire UI.

3. Floating Icons on Scroll:

Add lightweight, floating decorative background icons (e.g., wrench, hammer, lightning, briefcases using simple SVG or CSS shapes) that float gently with subtle animation as the user scrolls, staying within the dark theme styling.

4. Quick Support / Get Help Window (Frontend Only):

Add a floating circular 'Help / Support' button in the bottom-right corner.

Clicking it opens a compact slide-up support popup/modal with:

A quick FAQ or help message: "Need assistance? Reach our 24/7 SIH Hackathon desk at support@velocitygig.in or call +91-9876543210."

A simple text input for a query and a 'Send Message' button.

Do NOT connect this to the backend API. On clicking 'Send', just display a success message: "Thank you! Our support team will reach out shortly." and auto-close after 2 seconds.

Include a simple close (✕) button."*


